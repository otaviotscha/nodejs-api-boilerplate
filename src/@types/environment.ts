/**
 * Environment types.
 */
export enum EnvironmentType {
  DEV = 'development',
  TEST = 'test',
  PRODUCTION = 'production'
}

/**
 * .env file defaults.
 */
export enum EnvironmentDefault {
  PORT = '4000'
}
